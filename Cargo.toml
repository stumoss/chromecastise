[package]
edition = '2018'
name = "chromecastise"
version = "1.0.6"
authors = ["Stuart Moss <samoss@gmail.com>"]
build = "build.rs"

[build-dependencies.clap]
version = "^2"
default-features = false
features = ["suggestions"]

[dependencies.clap]
version = "^2"
default-features = false
features = ["suggestions"]

[dependencies]
num_cpus = "1.0"
log = "*"
